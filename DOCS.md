# API Docs

## Job Endpoints

- `GET /jobs` - Get All Jobs
- `GET /jobs/summary` - Get Job Summary
- `GET /jobs/dlq` - Get Dead Letter Queue
- `GET /jobs/{id}` - Get Single Job
- `PATCH /jobs/{id}/restart` - Restart Job

## App Endpoints

- `GET /app/settings` - Get App Settings
